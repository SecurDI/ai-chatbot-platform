"use client";

/**
 * Dashboard Layout
 * Simple layout wrapper for the SPA dashboard
 */
export default function DashboardLayout({
  children,
}: { children: React.ReactNode }) {
  return <>{children}</>;
}
